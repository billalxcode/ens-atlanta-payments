import { loadFixture } from "@nomicfoundation/hardhat-toolbox/network-helpers";
import hre from "hardhat"

describe("Payment", function() {
    async function deployPaymentContract() {
        const [owner] = await hre.ethers.getSigners()
        
    }
})